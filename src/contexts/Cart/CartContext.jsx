import { createContext } from "react";

// Create a new context object using createContext
const CartContext = createContext();

// Export the context object as the default export of this module
export default CartContext;
